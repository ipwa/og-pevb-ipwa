{% macro getClass(align) %}
  {% set classes = [
    'flex flex-col gap-5 md:gap-10 w-full pt-8',
    align == 'stretch' ? 'items-stretch',
    align == 'start' ? 'items-start',
    align == 'center' ? 'items-center',
    align == 'end' ? 'items-end',
  ] | join(' ') | trim %}
  {{ classes }}
{% endmacro %}
<div class="{{ _self.getClass(align) }}">
  {{ items }}
</div>
